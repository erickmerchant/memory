{
	"tasks": {
		"bundle": "deno bundle --platform browser --minify -o memory-game.min.js memory-game.js",
		"dev:bundle": "deno bundle --watch --platform browser --minify -o memory-game.min.js memory-game.js",
		"dev:serve": "coolstyleserver serve -p3000 .",
		"dev": {
			"dependencies": ["dev:bundle", "dev:serve"]
		}
	},
	"imports": {
		"handcraft": "npm:handcraft"
	},
	"fmt": {
		"useTabs": true
	}
}
