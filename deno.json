{
	"tasks": {
		"build": "deno bundle --platform browser --minify -o memory-game.min.js memory-game.js",
		"dev:app": "deno bundle --watch --platform browser --minify -o memory-game.min.js memory-game.js",
		"dev:styles": "coolstyleserver serve -p3000 .",
		"dev": {
			"dependencies": ["dev:app", "dev:styles"]
		}
	},
	"imports": {
		"handcraft": "npm:handcraft"
	}
}
